package br.com.kirgh.app;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * This is the main class for running the KirghEnergy application in Java.
 */
@SpringBootApplication
public class KirghEnergyApplication {
    /**
     * This is the main function that runs the KirghEnergy application in Java.
     */
    public static void main(String[] args) {
        SpringApplication.run(KirghEnergyApplication.class, args);
    }
}
